<% content_for :main_container_class, "flex items-center justify-center" %>

<turbo-frame id="main_content_area">
  <div class="text-center mt-20">


    <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-6xl">
      Welcome to 
      <span class="text-4xl font-bold bg-gradient-to-r to-secondary from-primary bg-clip-text text-transparent sm:text-6xl tracking-tight">
        natureHealer
      </span>
    </h1>

    <div class="max-w-200 mt-9">
      <p class="mt-6 text-xl leading-8 text-primary-content">
      I help you heal naturally from chronic health problems using the 5,000-year-old science of 
      <span data-tip="<%= @ayurveda_tooltip_text %>" class="tooltip text-2xl font-semibold bg-gradient-to-r to-accent from-primary bg-clip-text text-transparent tracking-wider wavy-text" data-controller="wavy-text">
        Ayurveda
      </span>
      to heal your body and mind, using just <span class="font-semibold">food</span>, <span class="font-semibold">herbs</span>, and <span class="font-semibold">natural cures</span>.
      </p>
    </div>

    <div class="mt-10 flex items-center justify-center gap-x-6">
      <%= form_with url: start_prakruti_quiz_path, method: :post, data: { turbo_frame: "main_content_area" } do %>
        <%= button_tag "Get started", class: "btn btn-primary text-white text-lg" %>
      <% end %>
      <a href="#" class="btn btn-soft btn-secondary text-lg">Learn more <span aria-hidden="true">→</span></a>
    </div>

  </div>
</turbo-frame>
