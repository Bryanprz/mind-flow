<% content_for :title, "Notifications - MindFlow" %>
<% content_for :hide_footer, true %>

<div class="w-full h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-black overflow-hidden">
  <div id="react-notifications" data-notifications-data="<%= @notifications_data.to_json %>" class="w-full h-full"></div>
</div>

<%= vite_javascript_tag 'notifications.jsx' %>
