<% content_for :main_container_class, 'w-full flex-1 flex flex-col bg-base-200' %>

<div class="drawer lg:drawer-open flex-1 flex overflow-hidden">
  <%= render 'sidebar' %>
  <div class="flex-1 overflow-auto px-4">
    <%= render 'dashboard_content' %>
  </div>
</div>
