<%# TODO 
make the dashboard diverse in its presentation of data

add instance vars
  - prakruti info (primary dosha)
  - vikruti info (current imbalance - chronic illnesses)
  - food (for recommending) (list or other)
  - herbs (for recommending)
  - cures (for recommending)
  - lifestyle (for recommending)

%>
<% content_for :main_container_class, 'w-full flex-1 flex justify-center items-stretch bg-base-200 p-8' %>

<div class="w-full mx-auto space-y-6 flex flex-col" data-controller="modal">
  <% if Current.user.prakruti %>
    <!-- First Row - 3 equal columns -->
    <div class="grid grid-cols-3 md:grid-cols-3 gap-6 h-full">

      <!-- Card 1: User Profile -->
      <div class="card bg-base-100 border border-gray-300">
        <div class="card-body">
          <h2 class="card-title text-lg">"Original Nature" Blueprint</h2>
          <p class="text-md">
            Your Original Nature is 
            <span class="font-bold <%= dosha_color_class(Current.user.prakruti) %>"><%= Current.user.prakruti.name.titleize %>.</span>
          </p>
          <p>
            <%= t("doshas.#{Current.user.prakruti.name.downcase}.description") %>
          </p>
        </div>
      </div>

      <!-- Card 2: Quick Actions -->
      <div class="card bg-base-100 border border-gray-300">
        <div class="card-body flex flex-col">
          <h2 class="card-title text-lg">Healing Plan</h2>
          <p class="flex-grow mb-4 mt-4"><%= t("doshas.#{Current.user.prakruti.name.downcase}.healing_plan_description") %></p>
          <% if Current.user.healing_plans.any? %>
            <%= link_to "View Healing Plan", my_healing_plan_path, class: "btn btn-accent text-lg w-full text-black mt-2 font-semibold" %>
          <% end %>

          <% if Current.user.vikruti_plans.empty? %>
            <%= form_with url: start_vikruti_assessment_path, method: :post, data: { turbo_frame: "main_content_area" } do %>
              <%= button_tag "Check My Balance", class: "btn btn-primary w-full text-white mt-2 font-semibold" %>
            <% end %>
          <% end %>
        </div>
      </div>

      <!-- Card 3: About Prakruti Dosha -->
      <div class="card bg-base-100 border border-gray-300">
        <div class="card-body">

          <h2 class="card-title text-lg ">
            About 
            <span class="<%= dosha_color_class(Current.user.prakruti) %>">
              <%= Current.user.prakruti.name.titleize %>
            </span>
          </h2>

          <ul class="list-disc pl-5 space-y-2 text-md">
            <% t("doshas.#{Current.user.prakruti.name.downcase}.bullet_points").each do |dosha_point| %>
              <li><%= dosha_point %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>



    <!-- Second Row - 2 equal columns -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 h-full">
      <!-- Card 4: Key Characteristics -->
      <div class="card bg-base-100 border border-gray-300">
        <div class="card-body">
          <h2 class="card-title text-lg">Key Characteristics</h2>
          <ul class="list-disc pl-5 space-y-2">
            <% t("doshas.#{Current.user.prakruti.name.downcase}.key_characteristics").each do |characteristic| %>
              <li><%= characteristic %></li>
            <% end %>
          </ul>
        </div>
      </div>

      <!-- Card 5: Balanced State -->
      <div class="card bg-base-100 border border-gray-300">
        <div class="card-body">
          <h2 class="card-title text-lg">Balanced State</h2>
          <div class="grid grid-cols-1 gap-4">
            <div>
              <h3 class="font-semibold text-md">When in Balance</h3>
              <p><%= t("doshas.#{Current.user.prakruti.name.downcase}.in_balance") %></p>
            </div>
            <div>
              <h3 class="font-semibold text-md">When Unbalanced</h3>
              <p><%= t("doshas.#{Current.user.prakruti.name.downcase}.out_of_balance") %></p>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% else %>
    <div class="card w-full max-w-lg mx-auto bg-base-100 border border-gray-300">
      <div class="card-body">
        <h2 class="card-title text-lg">"Original Nature" Blueprint</h2>
        <p class="text-md">
          Your Original Nature is the KEY to understanding how to go from a state of chronic illness to a state of constant health. Ayurveda calls your "Original Nature" the energetic blueprint that forms the foundation for your body, mind, and soul.
        </p>

        <span class="font-bold tex-accent">Start your Original Nature Assessment</span>
        <%= form_with url: start_prakruti_assessment_path, method: :post, data: { turbo_frame: "main_content_area" } do %>
          <%= button_tag "What is my Original Nature?", class: "btn btn-primary w-full text-white" %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
