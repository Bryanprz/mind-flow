<%# This template is rendered when the show action is called with a Turbo Stream request %>
<%= turbo_stream.replace "main_content_area" do %>
  <%= render "show" %>
<% end %>

<%= turbo_stream.update_url(user_path(@user)) %>
